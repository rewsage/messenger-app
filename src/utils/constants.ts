export enum PATHS {
	AUTH = "/auth",
	LOGIN = "/auth/login",
	SIGN_UP = "/auth/sign-up",
	PASSWORD_RESET = "/auth/reset",
	HOME = "/home",
}

export enum DB_NODES {
	USERS = "users/",
	CHATS = "chats/",
	CONVERSATIONS = "conversations/",
}
